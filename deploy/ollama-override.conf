# Drop-in de systemd para Ollama — instalar en:
# /etc/systemd/system/ollama.service.d/override.conf
# Fuerza que Ollama escuche SOLO en localhost (nunca exponer a internet).
[Service]
Environment="OLLAMA_HOST=127.0.0.1"
